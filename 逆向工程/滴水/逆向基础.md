### 程序是如何存储的？

通过UltraEdit（16进制/2进制文本编辑器）任意打开一个exe程序，可以看到程序在内存中是如何存储的：

![屏幕截图 2026-08-04 095429](D:\BaiduSyncdisk\文档\逆向工程\滴水\图片文件夹\屏幕截图 2026-08-04 095429.png)

1：所有的程序在内存中存储的形式都是二进制数字，但由于二进制用到的位置太多，所以许多软件都是会把2进制的数字用十六进制表示，即如图所示

###### 为什么每组都是两位十六进制？

因为计算机管理数据以字节为单位，因为八位二进制为一字节，一个十六进制对应四位二进制，两位十六进制刚好就是八位二进制刚好就是一个字节

###### 八进制的2-3等于多少？

1.计算机怎么存储数字？

2.什么叫数据的宽度？

```
场景 2：计算机固定长度无符号 / 定长补码运算（限定 N 位八进制、截断舍弃进位）计算机所有运算都有固定位宽，超出长度的高位直接丢弃。
以16 位八进制举例，定长范围内：
八进制 1 位对应 3 位二进制，n 位八进制对应 3n 位二进制。
定长补码中，\(-1\) 的补码永远是全 1 二进制，转八进制就是全 7。
推演：
二进制 8 位：11111111 → 转八进制 = 377
二进制 30 位（10 位八进制）：全部为 1 → 八进制7777777777
二进制极长位宽，全部二进制位都是 1，转为八进制就是一长串 7；
你写的1777...开头多了 1，一般是带符号位的超长八进制定长书写：
最高位 1 代表符号位，后面全部数值位为 7，整体就是定长补码下的 - 1。
八进制竖式定长减法演示（定长，借位无限向前借）八进制：2 - 3
本位 2 不够减 3，向前一位借 1，八进制借 1 当 8：
本位 = \(2+8-3=7\)
前一位被借走 1，也变成缺 1，继续向前无限借位，所有高位全部都变成 7。
在无限定长截断视角下，结果就是一串 7，前面加符号标记 1，也就是你说的177777...。二、结合你之前的两个问题复盘1、计算机怎么存储数字
底层只有二进制 0、1，没有八 / 十进制，八进制只是二进制简写；
有符号整数：内存存补码，减法 = 加负数补码；
定长存储：寄存器、内存变量都有固定比特长度，运算多出的高位直接丢弃（截断溢出）；
-1 在任意长度有符号补码里，二进制永远全 1，转八进制就是全 7。
2、数据宽度
定义：数据占用二进制 bit 的位数，决定运算的最大长度；
关键：运算严格受宽度约束
宽度越长，一串 7 的八进制位数就越多；
如果你设定极长的数据宽度，-1 的八进制补码就会出现一长串 7，也就是你看到的17777...；
无宽度限制的数学运算：结果就是 - 1；有固定宽度的计算机硬件运算：截断后为全 7 串。
```

###### 不同操作系统的可执行文件

由于历史遗留与公司竞争的原因，不同操作系统的可执行文件格式不同
window----PE文件
Linux--------ELF文件

### PE文件

[PE文件结构详解 --（完整版）_pe结构图下载-CSDN博客](https://blog.csdn.net/freeking101/article/details/102752048)

 PE（ Portable Execute）文件是Windows下可执行文件的总称，常见的有 DLL，EXE，OCX，SYS 等。它是微软在 UNIX 平台的 COFF（通用对象文件格式）基础上制作而成。最初设计用来提高程序在不同[ 操作系统](https://blog.csdn.net/freeking101/article/details/102752048#)上的移植性，但实际上这种文件格式仅用在 Windows 系列操作系统下。PE文件是指 32 位可执行文件，也称为PE32。64位的可执行文件称为 PE+ 或 PE32+，是PE(PE32)的一种扩展形式（请注意不是PE64)

###### 结构

PE文件的结构一般来说如下图所示：从起始位置开始依次是 DOS头，NT头，节表以及具体的节。 

Windows操作系统

![9c20506fdcd48d355199e19a00252b5e](D:\BaiduSyncdisk\文档\逆向工程\滴水\图片文件夹\9c20506fdcd48d355199e19a00252b5e.jpeg)

PE文件结构：
注释版：

![3a18d0544efcf37bb51479d3601e64c8](D:\BaiduSyncdisk\文档\逆向工程\滴水\图片文件夹\3a18d0544efcf37bb51479d3601e64c8.jpeg)


指向版：

![aa71d668ff4438d171b0158e6765f68f](D:\BaiduSyncdisk\文档\逆向工程\滴水\图片文件夹\aa71d668ff4438d171b0158e6765f68f.png)



###### PE文件的执行顺序

1. 当一个 PE 文件 被执行时，**PE 装载器** 首先检查 DOS header 里的 PE header 的偏移量。如果找到，则直接跳转到 PE header 的位置。
2. 当 PE装载器 跳转到 PE header 后，第二步要做的就是检查 PE header 是否有效。如果该 PE header 有效，就跳转到 PE header 的尾部。
3. 紧跟 PE header 尾部的是节表。PE装载器执行完第二步后开始读取节表中的节段信息，并采用文件映射（ 在执行一个PE文件的时候，Windows并不在一开始就将整个文件读入内存，而是采用与内存映射的机制，也就是说，**Windows装载器在装载的时候仅仅建立好虚拟地址和PE文件之间的映射关系，只有真正执行到某个内存页中的指令或者访问某一页中的[ 数据](https://blog.csdn.net/freeking101/article/details/102752048#)时，这个页面才会被从磁盘提交到物理内存**，这种机制使文件装入的速度和文件大小没有太大的关系 ）的方法将这些节段映射到内存，**同时附上节表里指定节段的读写属性**。
4. PE文件映射入内存后，PE装载器将继续处理PE文件中类似 import table （输入表）的逻辑部分。

### 从整体上了解一个EXE程序

![屏幕截图 2026-08-04 101748](D:\BaiduSyncdisk\文档\逆向工程\滴水\图片文件夹\屏幕截图 2026-08-04 101748.png)

1.程序从哪里开始？
![屏幕截图 2026-08-04 112044](D:\BaiduSyncdisk\文档\逆向工程\滴水\图片文件夹\屏幕截图 2026-08-04 112044.png)

###### 1.程序从哪里开始执行？

1.**十六进制的3c转为十进制就是60，即起始位置就存在第60个字节**，一行是0到f即16个，则第60个字节就在第4行倒数第四个，即如上图所示

2.WORD是两个字节，**DWORD是四个字节**，所以我们**想找的数据就是从第60个字节开始到64字节**，即上图中的绿圈框住的数据，即

```
E8 00 00 00
```

3.接下来需要对数据进行还原，由于windows操作系统是低位在前，高位在后(不是所有操作系统大都是这样)，因此还原后的数据即为

```
000000E8
```

**即，文件数据的起始位置为E8**

-----------------------------------------------------------前三步对应的就是DOS_HEADER板块(DOS头)----------------------------------

接下来按照PE的指向表【即PE文件的执行顺序】，我们进入了PE header板块(PE头)

4.看左手边的地址编号，向下按顺序直到**找到000000e0h,然后看该行第8个**，这里即为上面起始位置指向的**存储数据的起点**(注意并不是程序入口的起点)，如下图所示：
![屏幕截图 2026-08-04 115000](D:\BaiduSyncdisk\文档\逆向工程\滴水\图片文件夹\屏幕截图 2026-08-04 115000.png)