阿里云服务器：[云服务器管理控制台](https://ecs.console.aliyun.com/server/region/cn-beijing?__refreshToken=1741177746462)

**服务器账户密码：root Gzy13033418015**

通过xshell连接服务器

## 搭建

参考的是[vulfocus不能同步的解决方法/vulfocus同步失败_vulfocus无法同步-CSDN博客](https://blog.csdn.net/m0_64563956/article/details/131229046)

这篇文章

其中通过xftp修改了/etc/docker/daemon.json配置文件

其中的镜像网站用了最新的几个

- 这里每次使用时可能要时常更新

[国内能用的Docker镜像源【2025最新持续更新】_docker 镜像-CSDN博客](https://blog.csdn.net/u014390502/article/details/143472743?ops_request_misc=%7B%22request%5Fid%22%3A%227fd2e82ada7e5e326208b3d45fae0b65%22%2C%22scm%22%3A%2220140713.130102334..%22%7D&request_id=7fd2e82ada7e5e326208b3d45fae0b65&biz_id=0&utm_medium=distribute.pc_search_result.none-task-blog-2~all~top_positive~default-1-143472743-null-null.142^v101^pc_search_result_base2&utm_term=docker镜像源&spm=1018.2226.3001.4187)

拉取镜像命令改为：

docker pull vulfocus/vulfocus:latest

启动命令改为：

docker run -p 80:80 -v /var/run/docker.sock:/var/run/docker.sock -e VUL_IP=0.0.0.0 vulfocus/vulfocus

//前端运行

docker run -d -p 80:80 -v /var/run/docker.sock:/var/run/docker.sock -e VUL_IP=0.0.0.0 vulfocus/vulfocus

//后端运行

启动后通过浏览器访问服务器ip的80端口

**网站密码为admin Gzy13033418015**

```
sudo systemctl daemon-reload
 sudo systemctl restart docker
pull vulfocus/vulfocus:latest
```

## docker

```   
docker ps -a | grep vulfocus  # 查看所有 vulfocus 容器 
docker rm -f 容器ID          # 删除旧容器 
docker ps          # 查看正在运行的容器 ID 
docker stop <container_id>       # 停止容器（替换为实际容器 ID）
 docker images          查看镜像id
 docker run [镜像id]         启动镜像
```

